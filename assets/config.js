// ============================================================
// A FORMA — Configuração
// Preencha com os dados do seu projeto Supabase (Settings > API)
// A chave abaixo é a ANON/PUBLISHABLE key (pública por design;
// a segurança vem da política RLS "somente INSERT" na tabela leads)
// ============================================================
window.AFORMA_CONFIG = {
  SUPABASE_URL: "",      // ex.: "https://xxxxxxxx.supabase.co"
  SUPABASE_ANON_KEY: ""  // ex.: "sb_publishable_..." ou "eyJhbGciOi..."
};
